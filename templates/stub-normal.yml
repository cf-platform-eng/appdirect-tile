---
meta:
  environment: NAME
  service_name: NAME
  security_groups:
    - default
  job_templates:
    - name: AppDirect_broker
      release: AppDirect_broker
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID
releases:
  - name: AppDirect_broker
    version: latest
properties:
  AppDirect_broker: {}
